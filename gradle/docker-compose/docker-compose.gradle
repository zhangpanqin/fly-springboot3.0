apply plugin: 'com.avast.gradle.docker-compose'

dockerCompose {
    kafka {
        useComposeFiles = ["${rootProject.projectDir}/gradle/docker-compose/kafka-cluster.yaml"]
        projectName = 'kafka'
    }
}